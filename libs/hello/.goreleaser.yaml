version: 2

project_name: hello

builds:
  - skip: true

archives:
  - format: binary
  
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

monorepo:
  tag_prefix: libs/hello
  dir: libs/hello